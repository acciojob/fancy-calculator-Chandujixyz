//your JS code here. If required.
 var first=prompt("Take input for the first variable;");
  var mul=parseInt(first)*2;
  var second=prompt("Take another input for the second variable:");
  var result=mul+parseInt(second);
  alert("The final result is: " + result);